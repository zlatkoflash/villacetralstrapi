'use strict';

/**
 * the-apartments-ids service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::the-apartments-ids.the-apartments-ids');
